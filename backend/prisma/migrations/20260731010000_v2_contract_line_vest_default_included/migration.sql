-- 맞춤 정장의 기본을 3피스(베스트 포함)로 바꾼다 (현업 확정 2026-07-31).
--
-- 계약서 품목표의 체크박스도 [베스트 포함]에서 [베스트 제외]로 되돌아간다 —
-- 기본이 포함이므로 "제외할 때만 체크"가 읽는 대로 동작한다.
--
-- 컬럼 기본값만 바꾼다. 서비스(toLineData)가 값을 항상 명시해 넣으므로 이 기본값은
-- 직접 INSERT 하는 경우의 안전망이지만, 규칙과 스키마가 어긋나 있으면 나중에 함정이 된다.
--
-- 기존 라인은 건드리지 않는다 — 이미 확정된 계약의 금액이 그 값 기준으로 계산돼 있어
-- 지금 와서 베스트를 켜면 품목표와 합계가 어긋난다.
ALTER TABLE "contract_lines" ALTER COLUMN "vest_included" SET DEFAULT true;
